"""
# Assistant IA de Résumé + Quiz

Projet collaboratif : analyse de documents audio/texte avec génération de résumé, réponses et QCM.

## Structure
- `backend/` : API FastAPI
- `frontend/` : Interface utilisateur Streamlit
- `shared/` : Fichiers partagés (résumés, quiz en JSON)

## Lancement
1. Créer un venv + `pip install -r requirements.txt`
2. Lancer le backend : `uvicorn backend.main:app --reload`
3. Lancer Streamlit : `streamlit run frontend/app.py`
"""
